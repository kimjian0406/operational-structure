# 파이썬 코드도 운영체제 입장에서 보면 하나의 '프로세스'입니다.

import os

print("이 파이썬 스크립트는 하나의 프로세스로 실행되고 있어요.")
print("현재 실행 중인 프로세스의 ID(PID)는:", os.getpid())

# PID(Process ID)는 운영체제가 각 프로세스를 구분하기 위해 부여하는 고유한 번호입니다.
# 예시 출력: 현재 실행 중인 프로세스의 ID(PID)는: 11389

